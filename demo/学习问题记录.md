## 奇怪的问题
私有属性的访问：
命名下面的内容，在单独javac编译的时候报错，
但是在IDEA中不报错：
不知道为什么？？？？
（可能是装了什么插件）

```java
public class Main {
    public static void main(String[] args) {
        Test2 t2 = new Test2();
        t2.test1();
    }
}

class Test11 {
    public int a = 10;
    private int b = 111;
    protected int c;

    // 提供 getter/setter 方法访问 b
    // public int getB() {
    //     return b;
    // }

    // public void setB(int b) {
    //     this.b = b;
    // }
}

class Test2 {
    public void test1() {
        Test11 newTest1Instance = new Test11();
        int a = newTest1Instance.a;

        // ✅ 正确访问 private 字段 b
        // newTest1Instance.setB(11113);
        // int b = newTest1Instance.getB();

        // 不正确的方式
        int b = newTest1Instance.b;


        // ✅ 正确访问 protected 字段 c（同包）
        newTest1Instance.c = 3;
        int c = newTest1Instance.c;

        System.out.println("a: " + a);
        System.out.println("b: " + b);
        System.out.println("c: " + c);
    }
}


```